name: davianspace_http_ratelimit
description: Enterprise-grade HTTP rate limiting for Dart & Flutter with six algorithms, client/server pipelines, key-based admission control, and pluggable storage backends.
version: 1.0.0
homepage: https://github.com/davianspace/davianspace_http_ratelimit
repository: https://github.com/davianspace/davianspace_http_ratelimit
issue_tracker: https://github.com/davianspace/davianspace_http_ratelimit/issues
license: MIT

topics: 
  - rate-limiting
  - throttling
  - flutter
  - architecture
  - observability

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  davianspace_http_resilience: ^1.0.1
  meta: "^1.12.0"

dev_dependencies:
  fake_async: "^1.3.1"
  lints: "^6.1.0"
  test: "^1.25.2"
